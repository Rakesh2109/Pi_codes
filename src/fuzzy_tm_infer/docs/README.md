# Documentation

- `native_backends.md`: optional native Fuzzy TM backend notes.

Benchmark history is tracked outside the package in the legacy result archive.
The compact active native backend ledger lives in
`algorithms/c/fuzzy_tm/PERFORMANCE.md`.
